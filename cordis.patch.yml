# dsh bundle layer: mount the modlens-plus vision plugin (dsh/index.js via
# the package root export; bin stays the CLI, nothing imports the root).
- insert:
    - id: modlens-plus
      name: 'modlens-plus'
